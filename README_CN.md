# 流痕 (Flow Track)

[English Version](README.md)

> **捕捉操作瞬间，构建自动化律动。**

「流痕」是一款轻量级、高精度的桌面端定时自动化工具。它允许用户通过直观的界面预设一系列具有精确时间点的点击与文本粘贴任务，帮助您在复杂的连续性工作中解放双手。

![软件预览](assets/v2.0_ui_preview_cn.png)

## ✨ 核心特性

- **精准定时触发**：采用高保真三旋钮时间输入 (HH:MM:SS)，确保任务在预设时刻分毫不差地执行。
- **智能化坐标拾取**：集成实时坐标探测，一键捕捉目标位置。
- **高效任务序列流**：支持无限任务行，具备高级“复制设置”逻辑（秒数自动递增），实现任务极速创建。
- **显示桌面模式**：专属一键切换显示桌面功能，配合智能字段锁定，防止操作冲突。
- **智能配置持久化**：自动保存上次操作设定（包括窗口位置及自定义定时器）至 `config/` 目录。
- **全视觉反馈系统**：高对比度激活字段与新态按钮设计（激活渐变/禁用置灰），程序运行状态尽在掌握。
- **稳健的字段交互**：具备智能焦点锁定与鼠标滚轮事件拦截机制，有效防止滚动列表时的意外数据变更。
- **标准化构建流程**：完美的资源嵌入设计，配合标准化的 `main.spec` 配置文件，确保构建一致性。

## 🚀 快速开始

### 直接运行
1. 下载最新的发布包。
2. 运行 `dist/flow_track.exe`。
3. 如果是在新环境运行，软件会自动创建 `config/` 目录用于存储您的个人设定。

### 源码运行 (Python)
1. 确保安装了 Python 3.9+。
2. 安装依赖：
   ```bash
   pip install PySide6 qtawesome pywin32 pyperclip
   ```
3. 启动程序：
   ```bash
   python main.py
   ```

## 🛠️ 构建说明

本项目使用 PyInstaller 进行单文件封装。执行以下命令即可生成完全独立的 EXE：

```bash
pyinstaller main.spec --clean --noconfirm
```

## 📄 授权协议
本项目采用 [MIT](LICENSE) 许可证。
